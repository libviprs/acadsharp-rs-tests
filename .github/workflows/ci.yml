name: CI

# The Rust gate for this repo, the same four jobs and the same conventions as
# `acadsharp-rs`, which is the crate this suite exercises.
#
# Every job here lays `acadsharp-rs` down beside the workspace before it runs
# anything, through `./.github/actions/clone-counterpart`. That is not optional
# and not an optimisation: `Cargo.toml` depends on the crate as
# `{ path = "../acadsharp-rs" }`, so with no sibling cargo dies at manifest load
# and nothing at all compiles, `cargo fmt` aside. The action fetches exactly the
# sha in `COUNTERPART_REV`, as a sha, verifies the checkout landed on it, and
# refuses a pin that is not on the crate's `main`.
#
# It is a committed pin and not a repository variable, and it fails rather than
# falling back, because the org has paid for both shortcuts. `libviprs-org`
# resolved an unset repository variable to the empty string, which
# `actions/checkout` reads as "the default branch", so its gate compared against
# whatever moved that morning. `libviprs`' integration job clones a same-named
# counterpart branch, so a stale branch that shares a name builds against the
# wrong tree (libviprs#1013).
#
# `VIPRS_REQUIRE_COUNTERPART: 1` in each job is the other half: it turns "I
# cannot tell which acadsharp-rs this is" from a printed reason into a failure,
# so a sibling that arrived some other way cannot pass for the pinned one.
# `tests/counterpart_pin.rs` walks every job in this file and fails if one of
# them is missing either piece, so a job added later cannot quietly run without
# the counterpart.
#
# Jobs, all intended to be required checks on `main` once there is something to
# require them against:
#   1. Check & Lint  fmt plus clippy
#   2. MSRV          the declared floor, actually installed and checked
#   3. Docs          rustdoc with the warn-by-default link lints denied
#   4. Test          the suite

on:
  # A `push` run builds the branch head. It needs no merge commit, so it is
  # unaffected by whether the PR currently merges.
  push:
    branches: ['**']

  # `synchronize` is deliberately absent. A push to a branch in this repository
  # is already covered above, and adding `synchronize` would run the whole suite
  # twice for every one of those pushes. What is left are the pull-request
  # transitions that no push accompanies, which is also the only trigger that
  # covers a PR opened from a fork.
  pull_request:
    types: [opened, reopened, ready_for_review]

  workflow_dispatch:

# Only `push` runs supersede each other. That is narrower than it looks and the
# reasoning took two wrong turns to get right, so it is written out.
#
# A cancelled check run lands on the same head sha as the successful ones and
# GraphQL reports `isRequired: true` on it. What decides the merge is not that
# flag: branch protection evaluates the **latest** check run per context name,
# so a cancelled run blocks only when nothing later reran that same name.
#
# Measured on this repo rather than assumed. Opening PR #15 as a draft and
# marking it ready three seconds later produced three runs on one sha. The
# `opened` run was cancelled at :34; the `push` and `ready_for_review` runs
# finished at :55 and :58 with the same four job names. Result: every cancelled
# row `isRequired: true`, `statusCheckRollup.state` FAILURE, and
# `mergeStateStatus` CLEAN. The rollup aggregates every run; protection reads
# the last one per name. That gap is what makes a cancelled row look fatal when
# it is not, and what makes it genuinely fatal when it is.
#
# It is genuinely fatal when the run that did the cancelling does not produce a
# check run of the same name, so the cancelled row stays latest. A job rename
# in flight does exactly that, and it blocked three libviprs PRs whose bodies
# blamed the rename itself.
#
# Two earlier versions of this comment were wrong. The first called a cancelled
# check cosmetic; it is not. The second said keying the group on
# `github.event_name` fixes it; it does not, because `opened` and
# `ready_for_review` are both `pull_request` and still share a group, which is
# the case measured above. So the key keeps the event, and cancellation is
# limited to `push`, where a superseding run always reruns the same names.
# `pull_request` runs are rare here (no `synchronize`), so letting them all
# finish costs almost nothing and removes the whole class of confusion.
#
# `main` is excluded: that run is the record of what actually landed.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'push' && (github.head_ref || github.ref_name) != 'main' }}

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  check:
    name: Check & Lint
    runs-on: ubuntu-latest
    env:
      VIPRS_REQUIRE_COUNTERPART: 1
    steps:
      - uses: actions/checkout@v4
      # Right after checkout, because the pin file and `tools/counterpart.sh`
      # both come out of the checkout, and because nothing below this line can
      # even load the manifest without the sibling.
      - uses: ./.github/actions/clone-counterpart
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt -- --check
      - run: cargo clippy --all-targets -- -D warnings -W clippy::incompatible_msrv -W deprecated
      # This crate has no features yet. When it grows one, add a clippy cell for
      # it here rather than assuming the default pass covers it: feature-gated
      # code is invisible to the default cell, and in the core repo that hole
      # hid a real `collapsible_if` for months (libviprs#816).

  msrv:
    # Deliberately not "MSRV (1.97)". A version in a required check's name means
    # the next floor bump renames the context, the old name stays required, and
    # every PR on the repo blocks until someone edits branch protection
    # (libviprs#1011, libviprs-cli#57).
    name: MSRV
    runs-on: ubuntu-latest
    env:
      VIPRS_REQUIRE_COUNTERPART: 1
    steps:
      - uses: actions/checkout@v4
      # Right after checkout, because the pin file and `tools/counterpart.sh`
      # both come out of the checkout, and because nothing below this line can
      # even load the manifest without the sibling.
      - uses: ./.github/actions/clone-counterpart
      - uses: dtolnay/rust-toolchain@1.97
      - uses: Swatinem/rust-cache@v2
      # The floor is written in three places and nothing else compares them, so
      # they drift. `cargo check` cannot catch it, because checking with a newer
      # toolchain than you declare is not an error, and neither is a stale
      # sentence in a README.
      - name: Every MSRV claim agrees
        run: |
          declared=$(grep -m1 -E '^rust-version *= *"' Cargo.toml | sed -E 's/.*"([^"]*)".*/\1/' | cut -d. -f1,2)
          pinned=$(rustc --version | sed -E 's/^rustc ([0-9]+\.[0-9]+).*/\1/')
          advertised=$(grep -oE 'rust-[0-9]+\.[0-9]+%2B|Rust [0-9]+\.[0-9]+\+' README.md \
            | grep -oE '[0-9]+\.[0-9]+' | sort -u | paste -sd/ -)
          # Scoped to an actual `name:` line, not any mention. Grepping the
          # whole file matches the comment above that quotes the bad form, so
          # the guard fired on its own explanation the first time it ran.
          labelled=$(grep -oE '^[[:space:]]*name:[[:space:]]*MSRV[[:space:]]*\([0-9]+\.[0-9]+\)' .github/workflows/ci.yml | sort -u | paste -sd/ -)
          echo "declared (Cargo.toml): $declared"
          echo "pinned (this job):     $pinned"
          echo "advertised (README):   $advertised"
          rc=0
          [ -n "$advertised" ] || { echo "::error::no Rust version found in README.md, so this guard has nothing to compare"; rc=1; }
          [ "$declared" = "$pinned" ] || { echo "::error::this job installs $pinned but Cargo.toml declares rust-version $declared"; rc=1; }
          [ "$declared" = "$advertised" ] || { echo "::error::README advertises Rust $advertised+ but Cargo.toml declares $declared"; rc=1; }
          # Inverted on purpose. The other three clauses check that the copies
          # agree; this one checks that a version has not crept back into the
          # job name, which is the thing that makes the context un-renameable.
          [ -z "$labelled" ] || { echo "::error::the MSRV job name has a version in it ($labelled); keep it plain so a floor bump does not rename a required check"; rc=1; }
          exit $rc
      - run: cargo check --all-targets

  docs:
    # Also deliberately plain. Naming the denied lints in the context means
    # adding a fourth one renames a required check.
    name: Docs
    runs-on: ubuntu-latest
    env:
      # All three are warn-by-default, so without denying them rustdoc silently
      # drops a link and renders inert bracketed text on docs.rs while the job
      # stays green (libviprs#697, #795).
      RUSTDOCFLAGS: -D rustdoc::broken_intra_doc_links -D rustdoc::private_intra_doc_links -D rustdoc::redundant_explicit_links
      VIPRS_REQUIRE_COUNTERPART: 1
    steps:
      - uses: actions/checkout@v4
      # Right after checkout, because the pin file and `tools/counterpart.sh`
      # both come out of the checkout, and because nothing below this line can
      # even load the manifest without the sibling.
      - uses: ./.github/actions/clone-counterpart
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # All features, so a gated surface resolves instead of reading as broken
      # links. docs.rs itself builds with all-features.
      - run: cargo doc --no-deps --all-features

  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      VIPRS_REQUIRE_COUNTERPART: 1
    steps:
      - uses: actions/checkout@v4
      # Right after checkout, because the pin file and `tools/counterpart.sh`
      # both come out of the checkout, and because nothing below this line can
      # even load the manifest without the sibling.
      - uses: ./.github/actions/clone-counterpart
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # `tools/regenerate_reference.py` imports `compression.zstd`, which is
      # stdlib from 3.14 and absent before it, and the tool refuses to fall back
      # to a second zstd implementation because two code paths would produce two
      # different files. The runner image still ships 3.12, so the version is
      # pinned here rather than hoped for.
      #
      # This does NOT pull the reference generator into the Rust gate: nothing
      # below installs a .NET SDK, restores ACadSharp or reads a .NET artefact.
      # It is a Python version, for a Python tool whose tests are stdlib-only.
      # Remove the pin and the discovery below fails on the import, loudly,
      # which is the failure mode to want.
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      # tests/fixture_integrity.rs runs here, so the DWG corpus is checked by an
      # already-required context rather than by an advisory job nobody reads.
      # It needs no network, no .NET and no download: the committed corpus is
      # authoritative and CI never refreshes it.
      - run: cargo test --all-targets
      # `--all-targets` skips doctests, so they need their own line or the
      # examples in the public API documentation are compiled by nothing.
      - run: cargo test --doc
      # The same properties from the other side, and the one that catches a
      # manifest whose pin has stopped being a pin. Offline by construction.
      - name: Fixture corpus matches the manifest
        run: python3 tools/fetch_acadsharp_fixtures.py verify
      # The reference artefacts, checked against the manifest with no toolchain
      # at all: the hashes, the schema versions, the ACadSharp and SDK versions
      # the rows claim, and SUMMARY.md. It decompresses and never regenerates.
      - name: Reference outputs match the manifest
        run: python3 tools/regenerate_reference.py verify
      # The refusals both tools have to make. The fetch tool's are driven
      # through a local stub server rather than a mocked urlopen, because the
      # interesting behaviour is how urllib follows redirects and how much of a
      # body it reads, and a mock would only be asserting my own idea of that.
      # Stdlib unittest, so there is nothing to install.
      - name: The tools refuse what they should
        run: python3 -m unittest discover -s tools/tests -v
      # Every ACadSharp test file has a row, and every row has a file. Offline
      # against the committed snapshot, so an upstream outage cannot red this.
      # A bump that adds or renames a test file fails here until somebody
      # decides what it means, which is the whole point of the manifest.
      - name: Upstream test parity is classified
        run: python3 tools/parity_inventory.py
